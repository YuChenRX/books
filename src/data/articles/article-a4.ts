import type { Article } from '@/data/types'

const article: Article = // ─── 4. 微前端 ───
  {
    id: "a4",
    title: "微前端架构在大型企业项目中的落地实践",
    author: "架构师阿飞",
    date: "2026-06-19",
    views: "1.9k",
    likes: 62,
    comments: 19,
    tags: ["架构", "微前端"],
    avatar: "🏗️",
    excerpt: "微前端架构将微服务的设计理念引入前端开发，允许独立团队独立开发部署。本文从架构选型到落地实践全面分析...",
    content: [
      '<h2>一、微前端的核心问题</h2>',
      '<p>任何微前端方案都需要解决四个核心问题。应用加载与卸载，子应用之间不相互干扰包括 CSS 隔离、JavaScript 沙箱和 DOM 隔离。应用间通信需要规范且解耦的通信机制。公共依赖管理避免重复加载公共库导致性能浪费。统一的路由管理，主应用和子应用的路由需要协同工作。</p>',
      '<pre><code>// 微前端子应用生命周期约定\ninterface MicroAppLifecycle {\n  bootstrap: () => Promise<void>;      // 初始化，只会调用一次\n  mount: (props: AppProps) => Promise<void>;  // 挂载，每次进入子应用时调用\n  unmount: (props: AppProps) => Promise<void>; // 卸载，每次离开子应用时调用\n  update?: (props: AppProps) => Promise<void>;  // 可选，属性更新时调用\n}\n\n// 子应用注册示例\nconst microApp: MicroAppLifecycle = {\n  async bootstrap() {\n    console.log("子应用初始化完成");\n  },\n  async mount(props) {\n    console.log("子应用挂载，接收参数:", props);\n    // props.container: 挂载点 DOM 元素\n    // props.appName: 子应用名称\n    // props.baseRoute: 基础路由路径\n    ReactDOM.createRoot(props.container).render(<App />);\n  },\n  async unmount(props) {\n    console.log("子应用卸载");\n    ReactDOM.createRoot(props.container).unmount();\n  },\n};</code></pre>',
      '<p>目前主流的微前端方案包括基于路由分发、基于 iframe、基于 Web Components 和基于 Module Federation 四种模式。iframe 具有最好的隔离性但 URL 不同步和资源加载成本高。Module Federation 是 Webpack 5 推出的运行时模块共享方案。基于 Web Components 的方案利用 Custom Elements 和 Shadow DOM 实现组件级别隔离。</p>',
      '<blockquote><p>选择微前端方案时，最重要的考量因素不是技术本身，而是团队组织架构。康威定律指出"系统的架构受限于生产该系统的组织的沟通结构"。如果你的团队已经按照业务领域划分为独立的前端小组，微前端就是自然的技术选型。反之，如果前端团队规模较小，微前端引入的复杂性可能超过其价值。</p></blockquote>',
      '<h2>二、架构选型对比</h2>',
      '<p>qiankun 是目前国内应用最广泛的方案之一，优势在于技术栈无关、HTML entry 自动加载子应用资源、Snapshot 沙箱和 Proxy 沙箱隔离。Wujie 采用 WebComponent + iframe 双隔离机制。micro-app 使用类 WebComponent 协议通过 CustomEvent 实现通信。</p>',
      '<pre><code>// qiankun 主应用注册子应用\n// 主应用入口\nimport { registerMicroApps, start, initGlobalState } from "qiankun";\n\n// 注册子应用\nregisterMicroApps([\n  {\n    name: "app-order",          // 子应用名称\n    entry: "//localhost:3001",   // 子应用入口地址\n    container: "#sub-app-container",  // 挂载容器\n    activeRule: "/order",        // 激活规则\n    props: {                     // 传递给子应用的自定义参数\n      token: "xxx",\n      baseUrl: "/api",\n    },\n  },\n  {\n    name: "app-user",\n    entry: "//localhost:3002",\n    container: "#sub-app-container",\n    activeRule: "/user",\n    props: { token: "xxx" },\n  },\n]);\n\n// 启动 qiankun\nstart({\n  sandbox: { experimentalStyleIsolation: true },\n  prefetch: "all",  // 预加载所有子应用\n});\n\n// 全局状态共享\nconst { onGlobalStateChange, setGlobalState } = initGlobalState({\n  user: { id: 1, name: "Admin" },\n});\n\nonGlobalStateChange((state, prev) => {\n  console.log("全局状态变化:", state, prev);\n});</code></pre>',
      '<p>各方案在隔离机制上存在显著差异。qiankun 使用 Proxy 沙箱拦截 window 对象访问，在 mount 时创建代理、unmount 时恢复原始 window，实现了应用级别的 JavaScript 隔离。Wujie 利用 iframe 的原生隔离能力，通过 WebComponent 作为桥接层，在保证隔离性的同时解决了 iframe URL 不同步和通信不便的问题。micro-app 采用类似 WebComponent 的自定义元素协议，使用 Shadow DOM 进行样式隔离，通信则基于 CustomEvent 实现。</p>',
      '<ul>',
      '<li><strong>qiankun</strong>：技术栈无关、HTML entry 自动资源加载、Proxy/Snapshot 沙箱、社区生态完善</li>',
      '<li><strong>Wujie</strong>：WebComponent + iframe 双隔离、天然 JS 隔离、适合高安全场景</li>',
      '<li><strong>micro-app</strong>：类 WebComponent 协议、CustomEvent 通信、配置简洁</li>',
      '<li><strong>Module Federation</strong>：Webpack 5 原生、运行时模块共享、适合同一技术栈</li>',
      '</ul>',
      '<h2>三、落地实践</h2>',
      '<p>基座应用提供统一登录鉴权、全局布局和路由分发。子应用独立构建部署，通过约定的生命周期函数被主应用调用。通信推荐使用 CustomEvent 或共享全局状态 Store。CSS 隔离推荐 Shadow DOM 或 CSS Modules。JavaScript 隔离推荐 Proxy 沙箱技术。</p>',
      '<pre><code>// 子应用 webpack 配置 - 以 qiankun 为例\n// 子应用 webpack.config.js\nconst packageName = require("./package.json").name;\n\nmodule.exports = {\n  output: {\n    library: `${packageName}-[name]`,\n    libraryTarget: "umd",              // 必须为 UMD 格式\n    chunkLoadingGlobal: `webpackJsonp_${packageName}`,\n    globalObject: "window",\n  },\n  devServer: {\n    port: 3001,\n    headers: {\n      "Access-Control-Allow-Origin": "*",  // 允许跨域访问\n    },\n  },\n};\n\n// 子应用入口改造（src/index.tsx）\nlet root: ReactDOM.Root | null = null;\n\nfunction render(props: any) {\n  const { container } = props;\n  const dom = container\n    ? container.querySelector("#root")   // qiankun 提供的挂载点\n    : document.getElementById("root");\n  root = ReactDOM.createRoot(dom);\n  root.render(<App />);\n}\n\n// 独立运行时直接渲染\nif (!(window as any).__POWERED_BY_QIANKUN__) {\n  render({});\n}\n\n// 导出微前端生命周期\nexport async function bootstrap() {\n  console.log("子应用 bootstrap");\n}\nexport async function mount(props: any) {\n  render(props);\n}\nexport async function unmount() {\n  root?.unmount();\n  root = null;\n}</code></pre>',
      '<p>在微前端的性能优化方面，有几个关键的实践要点。子应用的预加载策略：qiankun 支持在浏览器空闲时预加载子应用资源，可以在 start 时配置 prefetch: "all" 或在用户 hover 到导航菜单时触发预加载。公共依赖的共享策略：通过 webpack 的 externals 配置将 React、Vue 等公共库提取为 CDN 加载，或者在 Module Federation 中配置 shared 依赖。子应用代码分割：每个子应用内部也应该按路由进行懒加载，避免一次加载整个子应用的代码。</p>',
      '<blockquote><p>微前端不是银弹。对于中小型项目，一个设计良好的 Monorepo 方案可能比微前端更高效。微前端的价值在团队规模超过 20 人、产品包含多个独立业务域、需要独立发版的情况下才能真正体现出来。在技术选型时，请务必评估团队在微前端运维方面的投入能力。</p></blockquote>',
    ].join("\n"),
  buryPoints: [1],
  },

export default article
