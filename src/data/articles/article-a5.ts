import type { Article } from '@/data/types'

const article: Article = // ─── 5. Node.js Event Loop ───
  {
    id: "a5",
    title: "深入理解 Node.js 事件循环机制与 Worker Threads",
    author: "Node 全栈",
    date: "2026-06-16",
    views: "2.3k",
    likes: 84,
    comments: 35,
    tags: ["Node.js", "后端"],
    avatar: "🚀",
    excerpt: "Node.js 的事件循环基于 libuv 实现了六个阶段的循环机制。理解这六个阶段对于写出高性能的 Node.js 代码至关重要...",
    content: [
      '<h2>一、事件循环六个阶段详解</h2>',
      '<p>Node.js 的事件循环基于 libuv 库实现了六个阶段的循环机制，这是其非阻塞 I/O 模型的核心支柱。理解这六个阶段对于写出高性能的 Node.js 服务端代码至关重要。每个阶段都有一个 FIFO 回调队列，事件循环会按顺序依次处理每个阶段，当前阶段队列清空或达到最大回调执行数后进入下一阶段。</p>',
      '<p>第一个阶段是 <strong>timers</strong>（定时器阶段），执行 setTimeout 和 setInterval 的回调。但需要注意的是，定时器的触发时间并不是精确的——如果事件循环当前正在处理 poll 阶段且耗时较长，定时器回调会被延迟执行。第二个阶段是 <strong>I/O callbacks</strong>（I/O 回调阶段），处理上一轮 poll 阶段未执行的 I/O 回调，以及一些系统操作的回调如 TCP 连接错误。第三个阶段是 <strong>idle, prepare</strong>（空闲/准备阶段），供 libuv 内部使用，开发者通常不会直接接触。</p>',
      '<p><strong>poll</strong>（轮询阶段）是最重要的阶段。它首先会检查是否有定时器到期，如果到期则回到 timers 阶段执行回调。然后 poll 阶段会获取新的 I/O 事件，执行其回调。如果 poll 队列为空，事件循环会检查是否有 setImmediate 回调（check 阶段），如果有则直接进入 check 阶段；如果没有，则等待新的 I/O 事件到来。这种等待机制使得 Node.js 能够在没有任务时进入休眠，不消耗 CPU 资源。</p>',
      '<p><strong>check</strong>（检查阶段）执行 setImmediate 的回调。setImmediate 和 setTimeout(fn, 0) 的区别是一个常见的面试题。在主模块中执行时，setTimeout(fn, 0) 的触发顺序取决于事件循环启动时的性能开销，因此顺序不确定。但在 I/O 循环内部，setImmediate 总是优先于 setTimeout(fn, 0)，因为 check 阶段在 timers 阶段之前。最后是 <strong>close callbacks</strong>（关闭回调阶段），处理 socket.on("close") 等关闭事件的回调。</p>',
      '<pre><code>// 演示事件循环各阶段的执行顺序\nconst fs = require("fs");\nconst path = require("path");\n\n// 读取文件触发 I/O 操作\nfs.readFile(__filename, () => {\n  // 1. 在 I/O 回调中：check 阶段优先于 timers 阶段\n  setTimeout(() => {\n    console.log("setTimeout (timer phase)");\n  }, 0);\n\n  setImmediate(() => {\n    console.log("setImmediate (check phase)");\n    // 输出顺序：setImmediate 先于 setTimeout\n    // 因为在 I/O 循环中，check 阶段在 timers 阶段之前\n  });\n\n  // 2. process.nextTick 优先级最高\n  process.nextTick(() => {\n    console.log("nextTick (微任务队列)");\n    // 在当前阶段结束后立即执行，早于所有 I/O 回调\n  });\n\n  // 3. Promise 微任务\n  Promise.resolve().then(() => {\n    console.log("Promise.then (微任务队列)");\n    // nextTick 优先于 Promise\n  });\n});\n\n// 输出顺序（I/O 循环内）：\n// nextTick (微任务队列)\n// Promise.then (微任务队列)\n// setImmediate (check phase)\n// setTimeout (timer phase)</code></pre>',
      '<blockquote><p>事件循环的每个阶段之间都会执行微任务队列（microtask queue）。process.nextTick 虽然名字带有"nextTick"但它并不是事件循环的一个阶段，而是一个微任务队列。Node.js 中 process.nextTick 的优先级高于 Promise.then，这与浏览器中微任务的执行顺序有所不同。如果递归调用 process.nextTick 会导致 I/O 回调永远无法执行，这就是 Node.js 推荐使用 setImmediate 替代 process.nextTick 的原因。</p></blockquote>',
      '<h2>二、微任务与 process.nextTick 深度剖析</h2>',
      '<p>Node.js 的微任务系统比浏览器更复杂。在事件循环的每个阶段之间，Node.js 会处理两个微任务队列：nextTickQueue 和 microTaskQueue（Promise 回调）。nextTickQueue 的优先级高于 microTaskQueue，这意味着在同一个阶段结束后，所有 process.nextTick 回调会先于 Promise.then 回调执行。这个设计决策源自 Node.js 早期的异步模型，但也因此带来了"递归 nextTick 导致 I/O 饥饿"的问题。</p>',
      '<pre><code>// process.nextTick 递归陷阱演示\nfunction recursiveNextTick() {\n  // 危险！这会导致事件循环永远无法进入 I/O 回调阶段\n  process.nextTick(() => {\n    console.log("不断执行 nextTick...");\n    recursiveNextTick();\n  });\n}\n\n// 正确做法：使用 setImmediate 进行递归\nfunction recursiveSetImmediate() {\n  // setImmediate 在 check 阶段执行，不会阻塞其他阶段\n  setImmediate(() => {\n    console.log("不断执行 setImmediate...");\n    recursiveSetImmediate();\n  });\n}\n\n// 微任务优先级对比\nsetTimeout(() => console.log("A: timer"), 0);\nsetImmediate(() => console.log("B: check"));\nprocess.nextTick(() => console.log("C: nextTick"));\nPromise.resolve().then(() => console.log("D: promise"));\n// 运行结果（主模块中）：\n// C: nextTick     ← 优先级最高\n// D: promise      ← 其次\n// A: timer 或 B: check  ← 取决于启动性能</code></pre>',
      '<p>在 async/await 代码中，await 后面的代码实际上会被包装为 Promise.then 微任务。因此在一个 async 函数中，await 之间的代码执行顺序也受到微任务队列的影响。理解这一点对于调试异步代码的执行顺序至关重要。</p>',
      '<h2>三、Worker Threads 多线程实战</h2>',
      '<p>Node.js 10 引入的 Worker Threads 为 JavaScript 带来了真正的多线程能力。每个 Worker 拥有独立的 V8 实例、事件循环和内存空间，可以并行执行 CPU 密集型任务而不阻塞主线程。Worker 线程与主线程通过消息传递（postMessage/onmessage）进行通信，数据使用结构化克隆算法（Structured Clone Algorithm）进行复制，这意味着包含函数和 Symbol 的对象无法直接传递。</p>',
      '<pre><code>// worker.js — 在 Worker 线程中执行 CPU 密集型计算\nconst { parentPort, workerData } = require("worker_threads");\n\n// 计算斐波那契数列（CPU 密集型）\nfunction fibonacci(n: number): number {\n  if (n <= 1) return n;\n  return fibonacci(n - 1) + fibonacci(n - 2);\n}\n\n// 接收主线程消息\nparentPort.on("message", (data) => {\n  const { taskId, n } = data;\n  const result = fibonacci(n);\n  // 发送结果回主线程\n  parentPort.postMessage({ taskId, result });\n});\n\n// 主线程代码 — main.js\nconst { Worker } = require("worker_threads");\n\nfunction runInWorker(n: number): Promise<number> {\n  return new Promise((resolve, reject) => {\n    const worker = new Worker("./worker.js", {\n      workerData: { n },\n    });\n\n    worker.on("message", (data) => {\n      resolve(data.result);\n      worker.terminate();\n    });\n\n    worker.on("error", reject);\n    worker.on("exit", (code) => {\n      if (code !== 0) {\n        reject(new Error(`Worker 退出码: ${code}`));\n      }\n    });\n  });\n}\n\n// 并行执行多个计算\nasync function main() {\n  const tasks = [40, 41, 42, 43];\n  const results = await Promise.all(tasks.map(runInWorker));\n  console.log("计算结果:", results);\n  // 通过 Worker 并行计算，充分利用多核 CPU\n}\n\nmain();</code></pre>',
      '<p>使用 Worker Threads 时需要注意几个关键事项。第一是线程间的通信开销——每次 postMessage 都会进行数据的序列化和反序列化，大数据量的频繁传递会成为性能瓶颈。推荐使用 SharedArrayBuffer 进行共享内存操作，避免数据复制。第二是 Worker 的创建开销——每个 Worker 需要启动独立的 V8 实例，创建成本约 2-5ms，因此不适用于短生命周期的任务，推荐使用 Worker 池来复用线程。第三是线程安全问题——尽管 JavaScript 是单线程语言，但 SharedArrayBuffer 的操作需要配合 Atomics 模块实现线程安全。</p>',
      '<h2>四、性能优化最佳实践</h2>',
      '<p>Node.js 性能优化的核心原则是避免阻塞事件循环。以下是在生产环境中经过验证的几条关键优化策略。第一，同步 CPU 密集型操作应移至 Worker Threads 或使用 child_process.fork 创建子进程。第二，避免在热路径中频繁创建和销毁 Promise 对象，可以使用对象池或缓存策略。第三，大量数据的处理应使用 Stream API 而非一次性加载到内存中。</p>',
      '<pre><code>// 使用 Stream API 处理大文件 — 避免内存溢出\nconst fs = require("fs");\nconst { Transform } = require("stream");\nconst zlib = require("zlib");\n\n// 创建可读流（逐块读取，不占用大量内存）\nconst readStream = fs.createReadStream("large-file.log");\nconst writeStream = fs.createWriteStream("large-file.log.gz");\n\n// 转换流：分析每行日志\ntransformStream = new Transform({\n  objectMode: false,\n  transform(chunk, encoding, callback) {\n    const lines = chunk.toString().split("\\n");\n    const filtered = lines\n      .filter((line) => line.includes("ERROR") || line.includes("WARN"))\n      .join("\\n");\n    callback(null, filtered);\n  },\n});\n\n// 管道连接：读取 → 过滤 → 压缩 → 写入\nreadStream\n  .pipe(transformStream)\n  .pipe(zlib.createGzip())\n  .pipe(writeStream)\n  .on("finish", () => {\n    console.log("日志处理完成，已压缩输出");\n  });\n\n// 事件监听器管理：使用 EventEmitter 时及时移除监听器\nconst EventEmitter = require("events");\nclass DataSource extends EventEmitter {}\n\nconst source = new DataSource();\nfunction onData(data) {\n  console.log("收到数据:", data);\n}\n\nsource.on("data", onData);\n// 业务逻辑...\n\n// 关键：不再需要时移除监听器，防止内存泄漏\nsource.off("data", onData);\n// 或者使用 once 替代 on，自动移除一次性监听器\nsource.once("end", () => {\n  console.log("数据流结束");\n});</code></pre>',
      '<p>内存泄漏是 Node.js 生产环境中最常见也最隐蔽的问题。典型的内存泄漏场景包括：全局变量意外持有大对象引用、闭包捕获了不需要的变量、定时器回调中引用了外部作用域的对象、事件监听器未正确移除、大量使用闭包和回调导致 V8 无法回收上下文。推荐使用 heapdump 或 v8-profiler 模块生成堆快照进行分析，使用 Chrome DevTools 的 Memory 面板可以直观地排查泄漏点。</p>',
      '<blockquote><p>在生产环境中建议设置环境变量 NODE_OPTIONS="--max-old-space-size=4096" 来控制 Node.js 进程的最大堆内存。同时使用 pm2 或 systemd 等进程管理工具实现进程守护和自动重启。对于 CPU 密集型服务，根据服务器 CPU 核心数创建 Worker 池，通常建议数量为 CPU 核心数减 1，保留一个核心给主线程和系统操作使用。</p></blockquote>',
    ].join("\n"),
  buryPoints: [1, 10],
  },

export default article
