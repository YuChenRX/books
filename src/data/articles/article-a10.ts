import type { Article } from '@/data/types'

const article: Article = // ─── 10. RxJS ───
  {
    id: "a10",
    title: "RxJS 响应式编程在复杂异步场景下的核心实践",
    author: "响应式编程",
    date: "2026-06-01",
    views: "1.6k",
    likes: 53,
    comments: 18,
    tags: ["RxJS", "异步"],
    avatar: "🌀",
    excerpt: "响应式编程在处理复杂异步场景时具有天然优势。RxJS 作为最流行的响应式编程库在前端领域有广泛应用...",
    content: [
      '<h2>一、Observable 核心概念与创建方式</h2>',
      '<p>RxJS 是一个用于处理异步事件流的响应式编程库，其核心抽象是 Observable（可观察对象）。Observable 代表一个可观察的数据流，它可以在时间上同步或异步地推送零个到无限个值。与 Promise 只能处理单个异步值不同，Observable 可以处理任意数量的值，并且提供了丰富的操作符来组合、转换和过滤这些值流。</p>',
      '<p>创建 Observable 有多种方式，每种适用于不同的场景。of 用于将一组已知的值逐个发出。from 可以将数组、Promise、可迭代对象或另一个 Observable 转换为流。fromEvent 用于将 DOM 事件或 Node.js 事件转换为流。interval 和 timer 用于创建周期性或延迟的流。new Observable 允许你自定义数据的推送逻辑，通过 subscriber.next()、subscriber.error() 和 subscriber.complete() 控制流的生命周期。</p>',
      '<pre><code>import { Observable, of, from, fromEvent, interval, timer } from "rxjs";\nimport { take, map, filter } from "rxjs/operators";\n\n// 1. 使用 of — 发出已知值\nof("Hello", "World", "RxJS").subscribe(console.log);\n// 输出: Hello, World, RxJS\n\n// 2. 使用 from — 从数组或 Promise 创建\nfrom([1, 2, 3, 4, 5]).pipe(\n  filter((n) => n % 2 === 0),\n  map((n) => n * 10)\n).subscribe(console.log);\n// 输出: 20, 40\n\nfrom(fetch("/api/users")).subscribe({\n  next: (response) => console.log(response),\n  error: (err) => console.error("请求失败", err),\n});\n\n// 3. 使用 fromEvent — DOM 事件流\nconst clicks$ = fromEvent(document, "click");\nconst throttledClicks$ = clicks$.pipe(\n  // 每 300ms 只取一次点击\n  map(() => new Date().toLocaleTimeString()),\n  // 去重相邻重复值\n  // distinctUntilChanged()\n);\n\n// 4. 使用 interval — 周期性流\nconst counter$ = interval(1000).pipe(\n  take(5)  // 只取前 5 个值\n);\ncounter$.subscribe((val) => console.log(`计数: ${val}`));\n// 输出: 计数: 0, 1, 2, 3, 4（每秒一个）\n\n// 5. 自定义 Observable — 完全控制推送逻辑\nconst custom$ = new Observable((subscriber) => {\n  let count = 0;\n  const id = setInterval(() => {\n    count++;\n    subscriber.next(`自定义值 ${count}`);\n    if (count >= 3) {\n      subscriber.complete();  // 完成流\n      clearInterval(id);\n    }\n  }, 500);\n\n  // 返回清理函数\n  return () => {\n    console.log("订阅已取消，清理定时器");\n    clearInterval(id);\n  };\n});</code></pre>',
      '<p>理解 Observable 的惰性执行（Lazy Execution）特性是使用 RxJS 的基础。Observable 在被订阅之前不会执行——声明 of([1,2,3]) 只是定义了一个数据流的蓝图，只有在 subscribe 被调用时才会开始推送数据。这与 Promise 的急切执行（Eager Execution）特性形成鲜明对比：new Promise 的构造函数在被创建时立即执行。这种惰性特性使得 Observable 天然适合表示随时间变化的值序列，例如用户输入、鼠标移动或 WebSocket 消息。</p>',
      '<blockquote><p>与 Observable 密切相关的概念是 Operator（操作符）。操作符是接受一个 Observable 并返回一个新的 Observable 的纯函数。它们通过 pipe 方法链式组合，每个操作符都接收上一个 Observable 发出的值，进行转换后发出新的值。这种函数式组合模式使得 RxJS 的代码具有极高的可组合性和可测试性——每个操作符都可以独立测试和重用。</p></blockquote>',
      '<h2>二、常用操作符实战</h2>',
      '<p>RxJS 提供了数百个操作符，但日常开发中常用的只有 20-30 个。根据功能可以划分为以下几类：<strong>转换类操作符</strong>（map、pluck、scan、buffer）将流中的数据转化为新形式；<strong>过滤类操作符</strong>（filter、take、skip、debounceTime、distinctUntilChanged）控制哪些值可以通过流；<strong>组合类操作符</strong>（merge、concat、combineLatest、forkJoin、withLatestFrom）将多个流合并为一个；<strong>高阶映射类操作符</strong>（switchMap、mergeMap、concatMap、exhaustMap）处理嵌套流——这是 RxJS 中最强大也最容易混淆的一类操作符。</p>',
      '<pre><code>import { Subject, from, of, interval } from "rxjs";\nimport {\n  map, filter, debounceTime, distinctUntilChanged,\n  switchMap, mergeMap, concatMap, exhaustMap,\n  catchError, takeUntil, retry, timeout,\n  combineLatest, withLatestFrom, forkJoin,\n} from "rxjs/operators";\n\n// 1. 搜索框防抖 — switchMap 经典应用\n// switchMap：每次新值产生时，取消上一个内部 Observable\n// 使用 debounceTime 实现防抖\nconst searchInput = document.getElementById("search")!;\nconst search$ = fromEvent(searchInput, "input").pipe(\n  map((e: InputEvent) => (e.target as HTMLInputElement).value),\n  debounceTime(300),           // 300ms 防抖\n  distinctUntilChanged(),       // 值没有变化时不请求\n  switchMap((query) =>\n    from(fetch(`/api/search?q=${query}`)).pipe(\n      timeout(5000),           // 5 秒超时\n      // 如果请求失败，返回空结果而不是让整个流崩溃\n      catchError(() => of({ results: [] }))\n    )\n  )\n);\n\nsearch$.subscribe((response) => {\n  renderResults(response.results);\n});\n\n// 2. 高阶映射操作符区别\n// switchMap：取消上一个，保留最新（搜索、自动补全）\n// mergeMap：同时保留所有，并行执行（批量上传）\n// concatMap：按顺序执行，一个一个来（表单提交）\n// exhaustMap：忽略新值直到当前完成（登录按钮防连点）\n\n// 3. 合并数据流\nconst userClicks$ = fromEvent(document, "click");\nconst mouseMoves$ = fromEvent(document, "mousemove");\n\n// combineLatest：所有源流都发出至少一个值后\n// 每次任一源流变化时，组合所有源流的最新值\ncombineLatest([userClicks$, mouseMoves$]).subscribe(\n  ([click, move]) => {\n    console.log("最新点击位置和鼠标位置:", click, move);\n  }\n);\n\n// forkJoin：等待所有源流完成，发出每个源的最后一个值\n// 类似于 Promise.all\nconst apiCall1$ = from(fetch("/api/users"));\nconst apiCall2$ = from(fetch("/api/posts"));\nforkJoin([apiCall1$, apiCall2$]).subscribe(\n  ([users, posts]) => {\n    console.log("所有请求完成:", users, posts);\n  }\n);</code></pre>',
      '<p>高阶映射操作符（High-order Mapping Operators）是 RxJS 学习曲线中最陡峭的部分。它们的共同特点是：接收一个值，返回一个内部 Observable（即"高阶"），然后将高阶级 Observable 拍平（flatten）为普通 Observable。区别在于当新值到来时，对上一个内部 Observable 的处理策略不同。switchMap 适合搜索自动补全——用户输入新关键词时旧请求应被取消。mergeMap 适合并发批处理——批量上传文件时不需要等待前一个上传完成。concatMap 适合顺序操作——表单按顺序提交。exhaustMap 适合防连点——首次点击正在处理时忽略后续点击。</p>',
      '<h2>三、Subject 与状态管理</h2>',
      '<p>Subject 是 RxJS 中一个特殊的类型，它同时实现了 Observable 和 Observer 两个接口，意味着它既可以作为数据流的源（通过 subscribe 被消费），也可以作为数据流的目标（通过 next、error、complete 向流中推送数据）。Subject 的独特之处在于它支持多播（Multicast）——同一个 Subject 可以被多个 Observer 订阅，当 Subject 接收到数据时，所有订阅者都会收到相同的数据。这与普通的 Observable 不同，后者通常是单播的——每个订阅者获得独立的数据流。</p>',
      '<p>RxJS 提供了四种 Subject 变体。Subject 是最基础的，不保存历史值。BehaviorSubject 始终保留最后一个值，新订阅者会立即收到当前值。ReplaySubject 可以缓存多个历史值，新订阅者会收到缓存的全部历史值。AsyncSubject 只发出最后一个值，且只在流完成时发出——类似于 Promise 的行为。在 Angular 的状态管理中，BehaviorSubject 是最常用的选择，因为它的"始终有值"特性非常适合表示应用状态。</p>',
      '<pre><code>import { Subject, BehaviorSubject, ReplaySubject, AsyncSubject } from "rxjs";\n\n// 1. Subject — 简单的多播\nconst subject$ = new Subject<number>();\n\n// 订阅者 A\nsubject$.subscribe((val) => console.log(`A: ${val}`));\nsubject$.next(1);  // A: 1\nsubject$.next(2);  // A: 2\n\n// 订阅者 B（加入较晚）\nsubject$.subscribe((val) => console.log(`B: ${val}`));\nsubject$.next(3);  // A: 3, B: 3\nsubject$.next(4);  // A: 4, B: 4\n// B 没有收到 1 和 2，因为它是在发送 1、2 之后才订阅的\n\n// 2. BehaviorSubject — 始终有值\nconst state$ = new BehaviorSubject<{ count: number }>({ count: 0 });\n\n// 新订阅者立即收到当前值\nstate$.subscribe((state) => console.log("当前状态:", state));\n// 输出: 当前状态: { count: 0 }\n\nstate$.next({ count: 1 });  // 输出: 当前状态: { count: 1 }\nstate$.next({ count: 2 });  // 输出: 当前状态: { count: 2 }\n\n// 3. ReplaySubject — 缓存历史\nconst replay$ = new ReplaySubject<number>(2);  // 缓存最近 2 个值\nreplay$.next(1);\nreplay$.next(2);\nreplay$.next(3);\n\nreplay$.subscribe((val) => console.log(`重放: ${val}`));\n// 输出: 重放: 2, 重放: 3（缓存了最近 2 个值）\n\n// 4. AsyncSubject — 只在完成时发出最后一个值\nconst async$ = new AsyncSubject<string>();\nasync$.subscribe((val) => console.log(`Async: ${val}`));\nasync$.next("不会发出");\nasync$.next("也不会发出");\nasync$.next("只在 complete 时发出这个");\nasync$.complete();  // 输出: Async: 只在 complete 时发出这个\n\n// 5. 使用 BehaviorSubject 实现简易状态管理\nclass Store {\n  private state$ = new BehaviorSubject<AppState>({ user: null, posts: [] });\n\n  getState() {\n    return this.state$.getValue();\n  }\n\n  select<T>(selector: (state: AppState) => T) {\n    return this.state$.pipe(map(selector));\n  }\n\n  dispatch(action: Action) {\n    const newState = reducer(this.getState(), action);\n    this.state$.next(newState);\n  }\n}</code></pre>',
      '<p>在 Angular 应用中，RxJS Subject 是组件间通信的主要方式。常用的模式包括：Service 中使用 BehaviorSubject 暴露状态，组件通过 subscribe 订阅状态变化；使用 Subject 实现跨组件的事件总线（Event Bus）；在路由守卫中使用 take(1) 或 first() 获取单次状态后自动取消订阅。在 React 应用中，RxJS 可以与 useObservable 等 Hook 结合使用，或者与 Redux-Observable 等中间件配合处理复杂的副作用逻辑。</p>',
      '<h2>四、错误处理与资源管理</h2>',
      '<p>异步编程中最让人头疼的问题之一是错误处理和资源清理，RxJS 为这两者都提供了优雅的解决方案。错误处理方面，RxJS 提供了 catchError（捕获错误并返回降级值）、retry（重试 N 次）、retryWhen（自定义重试策略）等操作符。资源管理方面，RxJS 的 subscribe 返回一个 Subscription 对象，调用 subscription.unsubscribe() 可以取消订阅并自动清理相关资源（如清除定时器、关闭 WebSocket 连接等）。</p>',
      '<pre><code>import { of, throwError, interval, Subject } from "rxjs";\nimport {\n  catchError, retry, retryWhen, delay, take,\n  takeUntil, finalize, tap, timeout,\n} from "rxjs/operators";\n\n// 1. 错误处理模式\nconst apiCall$ = from(fetch("/api/data")).pipe(\n  // 重试策略：最多重试 3 次\n  retry(3),\n  // 捕获错误并提供降级值\n  catchError((error) => {\n    console.error("请求失败，使用缓存数据", error);\n    return of({ data: "cached_data" });\n  }),\n  // 无论成功还是失败，最终都会执行\n  finalize(() => console.log("请求流程结束"))\n);\n\n// 2. 指数退避重试（Exponential Backoff）\nfunction exponentialBackoff(maxRetries = 3) {\n  return (source$) =>\n    source$.pipe(\n      retryWhen((errors$) =>\n        errors$.pipe(\n          // 每次错误时延迟递增\n          delay((attempt) => Math.min(1000 * Math.pow(2, attempt), 10000)),\n          // 统计重试次数\n          scan((count, error) => count + 1, 0),\n          // 超过最大重试次数后抛出错误\n          tap((count) => {\n            if (count > maxRetries) {\n              throw new Error("重试次数已用尽");\n            }\n          })\n        )\n      )\n    );\n}\n\n// 3. 自动取消订阅 — 防止内存泄漏\n// takeUntil 是最常用的自动取消模式\nconst destroy$ = new Subject<void>();\n\n// 组件初始化时订阅\nconst subscription = interval(1000).pipe(\n  takeUntil(destroy$)  // 当 destroy$ 发出值时自动取消\n).subscribe(console.log);\n\n// 组件销毁时触发取消（通常在 ngOnDestroy 中）\ndestroy$.next();\ndestroy$.complete();\n\n// 4. 超时控制\nfrom(fetch("/api/slow")).pipe(\n  timeout(3000),  // 3 秒超时\n  catchError((err) => {\n    if (err.name === "TimeoutError") {\n      return of({ timeout: true });\n    }\n    throw err;\n  })\n);\n\n// 5. 使用 Subscription.add() 管理多个订阅\nconst subscriptions = new Subscription();\n\nsubscriptions.add(\n  observable1$.subscribe(/* ... */)\n);\nsubscriptions.add(\n  observable2$.subscribe(/* ... */)\n);\n\n// 一次性取消所有订阅\nsubscriptions.unsubscribe();</code></pre>',
      '<p>在真实的 Angular 或 React 应用中，推荐使用 takeUntil 模式来管理组件的订阅生命周期。在组件类中声明一个私有的 destroy$ Subject，在 ngOnDestroy 或 useEffect 的清理函数中调用 destroy$.next()。所有的订阅都通过 takeUntil(destroy$) 来限定生命周期，这样当组件销毁时所有订阅自动取消，避免内存泄漏。这是 React 函数组件中使用 RxJS 时最推荐的模式。</p>',
      '<blockquote><p>RxJS 的学习曲线确实陡峭，但一旦掌握，它在处理复杂异步场景时的表达能力是其他方案难以比拟的。一个典型的例子是"实时搜索建议"场景：需要同时处理用户输入防抖、请求取消、超时控制、错误重试和结果缓存。如果用 Promise + async/await 实现，代码量大约是 RxJS 方案的 3-4 倍，而且边界条件的处理更加困难。RxJS 的 pipeable 操作符组合使得每个关注点都可以独立实现和测试。</p></blockquote>',
    ].join("\n")
  buryPoints: [1, 11],
  },
];

export default articles;

export default article
